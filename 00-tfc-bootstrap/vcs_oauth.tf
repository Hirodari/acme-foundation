# Root module (00-tfc-bootstrap directory)
# data "tfe_oauth_client" "client" {
#   organization = var.tfc_organization
#   name         = var.github_oauth_client  # must match the VCS provider display name in TFC, e.g. "GitHub"
# }